import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.independo.inderun.example",
  appName: "IndeRun Bridge Example",
  webDir: "dist"
};

export default config;
