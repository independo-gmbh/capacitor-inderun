# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo

# Swift / SwiftPM
.build/
.swiftpm/
Package.resolved

# Android / Gradle
android/.gradle/
android/build/
android/local.properties
android/.gradle-home/
android/captures/
.gradle/
build/

# OS / editor
.DS_Store
*.log
.idea/

# Example app (unpublished; native projects are generated by `cap add`)
example-app/node_modules/
example-app/dist/
example-app/ios/
example-app/android/
example-app/.env
example-app/.env.*
# Written by `pnpm install --ignore-workspace` inside example-app. The lockfile has no CI
# validating it, and the generated workspace file is a copy of the root's
# minimumReleaseAgeExclude — committing either just creates a second thing to keep in sync.
example-app/pnpm-lock.yaml
example-app/pnpm-workspace.yaml
